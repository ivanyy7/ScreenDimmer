---
description: Московское время (MSK) для записей в PROJECT_LOG и похожих файлах
alwaysApply: true
---

# Правило: время в логе проекта — московское (MSK)

## Базовая договорённость

- Для записей в **`PROJECT_LOG.md`** и других проектных файлах с меткой **`День N, ЧЧ:ММ`** используется **московское время**: **MSK (UTC+3)**, в Windows — часовой пояс **`Russian Standard Time`**.
- **Не** подставлять «локальное время компьютера пользователя» как московское: у пользователя может быть другой часовой пояс. Если нужна **точная** метка «сейчас» — получать **именно MSK** (см. ниже).

## Приоритет источника времени

1. Явное время в **сообщении пользователя** («сейчас 00:15 по Москве» и т.п.) — использовать его как есть (в 24-часовом виде в записи).
2. Если пользователь не указал время, а нужна актуальная метка — **московское время**: выполнить в терминале **одну** команду PowerShell из блока ниже и взять `ЧЧ:ММ` из результата.
3. Не выдумывать время и **не** использовать произвольное `12:00` как заглушку.

## Команда для точного MSK (PowerShell, Windows)

Копировать одну строку (из корня репозитория не требуется):

```powershell
[System.TimeZoneInfo]::ConvertTimeBySystemTimeZoneId([DateTime]::UtcNow, 'Russian Standard Time').ToString('HH:mm')
```

Полная дата и время по Москве (если нужно в другом файле):

```powershell
[System.TimeZoneInfo]::ConvertTimeBySystemTimeZoneId([DateTime]::UtcNow, 'Russian Standard Time').ToString('yyyy-MM-dd HH:mm')
```

## Связь с другими правилами

- Формат **`ЧЧ:ММ`**, ночные часы, запрет подмены ночи на полдень — см. **ПРАВИЛА_PROJECT_LOG_AND_MAP.mdc** (раздел «Формат записей истории»). Это правило **дополняет** их: **какое** время брать — **московское**, если не оговорено иное.
