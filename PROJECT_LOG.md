# PROJECT_LOG — ScreenDimmer

## Текущий статус

- **Стабильный MVP + этап 2:** затемнение по **F8**, трей, **ЛКМ** открывает/скрывает инфо-окно (фикс: `MouseDown` вместо `MouseClick`), тема и геометрия в JSON, выход только **Exit**, single instance. Поведение подтверждено пользователем.
- **Сборка:** .NET 8 SDK; перед `dotnet build` при необходимости закрывать запущенный exe. **Release** проверена.
- **В фокусе:** **этап 3** (упаковка для переноса на другие ПК: `publish` self-contained / установщик) — **отложен**; при желании позже — горячая клавиша через конфиг.

## Ключевые шаги и история

- День 1, 12:00 — Создан MVP: структура WPF, `LowLevelKeyboardHook`, `DimmerService`, `OverlayWindow`, `NotifyIconService`, single instance, манифест asInvoker.
- День 1, 12:05 — Горячая клавиша переведена с F6 на **F8** (`vkCode == 0x77`), тексты в трее обновлены («Toggle Dimmer (F8)»).
- День 1, 12:10 — Добавлены `PLAN.md`, `PROJECT_MAP.md`, `PROJECT_LOG.md` для фиксации состояния и маршрута.
- День 1, 15:00 — Проверены **сборка Release** и ручной запуск exe: затемнение по F8 и снятие затемнения работают; поведение мыши по чёрному экрану согласовано (менять на click-through не требуется).
- День 1, 16:00 — Добавлены **своя иконка** трея и exe (`Assets/app.ico`), пункт меню **«Автозапуск при входе в Windows»** (ярлык в пользовательской автозагрузке, без HKLM).
- День 1, 16:30 — Подтверждено: иконка в проводнике и в трее; отладочное логирование и `AgentDebugLog` удалены.
- День 1, 17:00 — Синхронизированы **PLAN / PROJECT_MAP / PROJECT_LOG** по итогам сессии; зафиксированы скрипты `tools/rebuild-app-ico.ps1`, диагностика иконки при необходимости.
- День 2, 00:15 — В **PLAN.md** зафиксирован **этап 2** (окно настроек/инфо, тёмная/светлая тема, поведение трея и панели задач); кратко в логе — см. раздел «План дальше» и **PLAN.md**.
- День 2, 00:51 — Промт финала сессии: синхронизированы **PROJECT_LOG** и **PROJECT_MAP** с **PLAN.md** (этап 2: senior, подэтапы 2.1–2.7); зафиксирована готовность к реализации; время записи — **MSK** (см. `.cursor/rules/ВРЕМЯ_MSK_PROJECT_LOG.mdc`).
- День 2, 00:55 — Подэтап **2.1**: добавлены `MainWindow.xaml` / `MainWindow.xaml.cs` (каркас ~480×320, тёмная тема, тексты, F8), временный `Show()` из `App.xaml.cs` по **PLAN.md**; обновлена **PROJECT_MAP**. Сборка: при занятом `ScreenDimmer.exe` — закрыть процесс или `dotnet build -o artifacts\verify-build`.
- День 2, 01:00 — Подэтап **2.2**: `WindowStyle=None`, `WindowChrome`, шапка с перетаскиванием, одна кнопка «—» → `Hide`; `Closing` отменяется + `Hide`; при `Minimized` — сброс и `Hide`. **MSK**.
- День 2, 01:02 — Подэтап **2.3**: `RadioButton` «Тёмная»/«Светлая», `ApplyTheme()` в `MainWindow` (кисти WPF, без конфликта с `System.Drawing` в проекте WinForms). Сохранение темы — **2.4**.
- День 2, 01:30 — Подэтап **2.4**: `UserSettingsData` / `UserSettingsStore` (JSON в `%LocalAppData%\ScreenDimmer\settings.json`), загрузка при старте окна, сохранение при смене темы, при перемещении/resize (debounce 400 ms), при скрытии окна; `ClampWindowToVirtualScreen` после `Loaded`. **MSK**.
- День 2, 01:36 — Подэтап **2.5**: убран `Show()` при старте в `App.xaml.cs`; окно только создаётся, при запуске виден трей и работает F8. **MSK**.
- День 2, 01:39 — Подэтап **2.6**: `NotifyIcon.MouseClick` (ЛКМ) → `MainWindowToggleRequested`; в `App` — `Dispatcher.Invoke(ToggleMainWindowSafe)`, `Show`/`Hide`/`Activate`. **MSK**.
- День 2, 01:41 — Подэтап **2.7** (финальная приёмка): проверены критерии этапа 2 из **PLAN.md** — ЛКМ/«—»/закрытие окна без кнопки в панели задач, сохранение темы и геометрии, **Exit** как единственный выход, F8 и автозапуск в фоне, второй экземпляр блокируется **Mutex**; обновлены **PLAN** (статус этапа 2), **PROJECT_MAP**, **README**; подсказка трея уточнена («ЛКМ — окно настроек»). **MSK**.
- День 3, 02:17 — **Баг:** ЛКМ по иконке трея не открывал окно (в т.ч. у «скрытых» иконок). **Исправление:** в `NotifyIconService` — **MouseDown** (ЛКМ) вместо **MouseClick**; в `App` — `Dispatcher.BeginInvoke(ToggleMainWindowSafe)`; при показе окна — `WindowState.Normal`, краткий `Topmost` + `Activate`. Поведение проверено. **MSK**.
- День 3, 02:18 — Промт финала сессии (**1_ФИНАЛ_СЕССИИ**): синхронизированы **PROJECT_LOG** и **PROJECT_MAP**; **этап 3** (упаковка/установщик) **отложен**. **MSK**.

## Технические решения и грабли

- **RegisterHotKey** не подходит для комбинаций с **Fn** — используется **WH_KEYBOARD_LL** и отслеживание нужного `vkCode` на KEYUP, чтобы не ловить автоповтор.
- На части ноутбуков ОС не различает «голую» F8 и Fn+F8 — поведение зависит от OEM; при необходимости можно ужесточить фильтр по `flags` в `KBDLLHOOKSTRUCT`.
- Сборка через `dotnet` на машине без установленного SDK завершается ошибкой «No .NET SDKs were found» — лечится установкой SDK 8.x (например 8.0.419), перезапуск терминала после установки обычно достаточен; полная перезагрузка ПК не обязательна.
- Установщик SDK предупреждает о **Visual Studio 2022 ≥ 17.8.3** при работе с .NET 8 *внутри VS* — на использование SDK из Cursor/CLI это не накладывает обязательных ограничений.
- Автозапуск через ярлык в `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup` — не требует прав администратора; при запущенном exe **повторная сборка Release** может падать из‑за блокировки файла (закрыть приложение перед `dotnet build`).
- Иконка: один `app.ico` в проекте для exe и трея. Старый ICO из конверта PNG давал `ArgumentException` у `new Icon()` (GDI+ не принимал формат) — трей падал на системную иконку; исправлено пересборкой через `tools/rebuild-app-ico.ps1`. Для произвольного PNG — `tools/png-to-ico.ps1 -InputPng ...` (проверять загрузку `Icon` вручную).
- **WinForms `NotifyIcon`:** для ЛКМ из трея надёжнее **`MouseDown`**, чем **`MouseClick`**, если нужна работа и из меню переполнения «скрытых» иконок — полный клик там часто не доходит до `MouseClick`.

## План дальше

- **Этап 3** (упаковка: `dotnet publish` self-contained, при желании установщик Inno Setup / аналог) — **отложен**, в репозитории пока не оформлен.
- По желанию: настройка горячей клавиши через конфиг (без пересборки).
